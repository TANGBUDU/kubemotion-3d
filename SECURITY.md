# Security policy

KubeMotion 0.1 is a static teaching site. It must not accept cluster credentials, connect to a Kubernetes API, expose a terminal, or perform cluster writes.

Report a suspected vulnerability privately through the repository's security advisory feature. Do not include real credentials, cluster names, internal addresses, or production data in a report or reproduction.
