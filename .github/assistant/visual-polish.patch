--- a/src/renderer/visuals/PodVisual.ts
+++ b/src/renderer/visuals/PodVisual.ts
@@ -62,7 +62,7 @@
       new THREE.LineBasicMaterial({
         color: palette.dataFlow,
         transparent: true,
-        opacity: 0.72,
+        opacity: 0.88,
         depthWrite: false,
         toneMapped: false,
       }),
@@ -94,7 +94,13 @@
 
     const bayGeometry = this.ownGeometry(createRoundedBoxGeometry(1.43, 0.075, 0.98, 0.08));
     const bayMaterial = this.ownMaterial(
-      createSurfaceMaterial({ tone: 'recessed', roughness: 0.72, metalness: 0.03 }),
+      createSurfaceMaterial({
+        color: palette.runtimeModule,
+        roughness: 0.68,
+        metalness: 0.03,
+        transparent: true,
+        opacity: 0.48,
+      }),
     );
     const bayFloor = new THREE.Mesh(bayGeometry, bayMaterial);
     bayFloor.position.y = 0.28;
--- a/src/styles/global.css
+++ b/src/styles/global.css
@@ -318,17 +318,18 @@
 }
 .scene-layout-label {
   z-index: 2;
-  padding: 3px 7px;
-  border-color: rgb(139 211 255 / 28%);
-  background: rgb(6 15 28 / 72%);
-  color: #a9dfff;
-  font-size: 10px;
-  font-weight: 760;
-  letter-spacing: 0.11em;
+  padding: 2px 5px;
+  border-color: transparent;
+  background: rgb(6 15 28 / 42%);
+  color: #8ecdf2;
+  font-size: 9px;
+  font-weight: 720;
+  letter-spacing: 0.1em;
   text-transform: uppercase;
+  text-shadow: 0 1px 8px rgb(0 0 0 / 68%);
 }
 .scene-layout-label.scene-zone-title {
-  border-left: 2px solid rgb(139 211 255 / 72%);
+  border-left: 2px solid rgb(139 211 255 / 46%);
 }
 .scene-layout-label.scene-tray-title {
   border-color: rgb(240 180 77 / 44%);
@@ -336,28 +337,30 @@
 }
 .scene-route-label {
   z-index: 5;
-  border-color: rgb(200 188 255 / 66%);
-  background: rgb(18 14 43 / 88%);
-  color: #e3dcff;
-  font-size: 10px;
-  font-weight: 760;
-  letter-spacing: 0.04em;
-  text-transform: lowercase;
+  padding: 2px 5px;
+  border-color: rgb(200 188 255 / 22%);
+  background: rgb(13 18 36 / 64%);
+  color: #c9c4e8;
+  font-size: 9px;
+  font-weight: 650;
+  letter-spacing: 0;
+  text-transform: none;
+  box-shadow: 0 2px 10px rgb(0 0 0 / 18%);
 }
 .scene-callout {
   position: absolute;
   z-index: 8;
   top: 0;
   left: 0;
-  max-width: 300px;
-  padding: 8px 11px;
+  max-width: 240px;
+  padding: 6px 8px;
   border: 1px solid #8bd3ff;
   border-radius: 8px;
   background: rgb(7 22 38 / 96%);
   color: #eef8ff;
   box-shadow: 0 10px 34px rgb(0 0 0 / 36%);
   font:
-    700 11px/1.35 ui-monospace,
+    650 10px/1.35 ui-monospace,
     SFMono-Regular,
     Consolas,
     monospace;
--- a/src/renderer/SceneRegistry.ts
+++ b/src/renderer/SceneRegistry.ts
@@ -302,10 +302,13 @@
     Math.max(0.05, container.bounds.size[1]),
     container.bounds.size[2],
   );
+  const subtleTrafficGuide =
+    container.kind === 'semantic-lane' &&
+    /^(traffic-|external-traffic-|rollout-traffic-|dns-|gateway-)/.test(container.id);
   const material = new THREE.MeshBasicMaterial({
     color: guideColor(container),
     transparent: true,
-    opacity: 0.045,
+    opacity: subtleTrafficGuide ? 0.018 : 0.045,
     depthWrite: false,
   });
   const surface = new THREE.Mesh(geometry, material);
@@ -316,9 +319,9 @@
   const edgeMaterial = new THREE.LineDashedMaterial({
     color: guideColor(container),
     transparent: true,
-    opacity: 0.72,
-    dashSize: 0.5,
-    gapSize: 0.22,
+    opacity: subtleTrafficGuide ? 0.24 : 0.72,
+    dashSize: subtleTrafficGuide ? 0.34 : 0.5,
+    gapSize: subtleTrafficGuide ? 0.34 : 0.22,
   });
   const border = new THREE.LineSegments(edgeGeometry, edgeMaterial);
   border.computeLineDistances();
