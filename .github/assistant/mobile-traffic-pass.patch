--- a/src/renderer/LabelManager.ts
+++ b/src/renderer/LabelManager.ts
@@ -60,7 +60,7 @@
 const DESKTOP_LABEL_LIMIT = 7;
 const MOBILE_LABEL_LIMIT = 3;
 const DESKTOP_ROUTE_LABEL_LIMIT = 3;
-const MOBILE_ROUTE_LABEL_LIMIT = 1;
+const MOBILE_ROUTE_LABEL_LIMIT = 0;
 // Browser font metrics can resolve a fraction wider than offsetWidth after a locale/text update.
 // Reserve a few CSS pixels so a label accepted at the safe-rect edge remains inside it when
 // getBoundingClientRect() observes the final rendered glyphs.
--- a/src/renderer/layouts/traffic/InternalServiceTrafficLayout.ts
+++ b/src/renderer/layouts/traffic/InternalServiceTrafficLayout.ts
@@ -19,7 +19,7 @@
 const MAIN_Z = 0.5;
 const ENDPOINT_SLICE_X = 1.35;
 // The Traffic camera looks from positive Z, so foreground support projects below the corridor.
-const ENDPOINT_SLICE_Z = 3.8;
+const ENDPOINT_SLICE_Z = 3.0;
@@ -68,11 +68,11 @@
       position: (_entity, index, count) => [
         CLIENT_X,
         0.18,
-        MAIN_Z + (index - (count - 1) / 2) * 2.35,
+        MAIN_Z + (index - (count - 1) / 2) * 1.8,
       ],
       bounds: {
         center: [CLIENT_X, 0.025, MAIN_Z],
-        size: [2.8, 0.05, Math.max(3.4, clients.length * 2.35 + 0.9)],
+        size: [2.8, 0.05, Math.max(3.0, clients.length * 1.8 + 0.8)],
       },
       labelAnchor: [CLIENT_X - 1.3, 0.1, MAIN_Z - 1.6],
@@ -91,13 +91,13 @@
       position: (_entity, index, count) => [
         BACKEND_X,
         0.18,
-        MAIN_Z + (index - (count - 1) / 2) * 2.35,
+        MAIN_Z + (index - (count - 1) / 2) * 1.8,
       ],
       bounds: {
         center: [BACKEND_X, 0.025, MAIN_Z],
-        size: [3.8, 0.05, Math.max(3.4, backends.length * 2.35 + 0.9)],
+        size: [3.8, 0.05, Math.max(3.0, backends.length * 1.8 + 0.8)],
       },
-      labelAnchor: [BACKEND_X - 1.75, 0.1, MAIN_Z - Math.max(1.6, backends.length * 1.2)],
+      labelAnchor: [BACKEND_X - 1.75, 0.1, MAIN_Z - Math.max(1.45, backends.length * 0.92)],
     });
--- a/tests/renderer/labels.test.ts
+++ b/tests/renderer/labels.test.ts
@@ -739,7 +739,7 @@
     manager.update(registry, camera(), 800, 500);
     expect(labels.filter((element) => !element.hidden)).toHaveLength(3);
     manager.update(registry, camera(), 390, 844);
-    expect(labels.filter((element) => !element.hidden)).toHaveLength(1);
+    expect(labels.filter((element) => !element.hidden)).toHaveLength(0);
     manager.clear();
   });
