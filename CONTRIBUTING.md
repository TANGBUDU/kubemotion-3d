# Contributing

Use Node.js 24 and pnpm. Keep Kubernetes teaching claims tied to official sources, keep all three locales complete, and run the full validation suite before opening a change.

Content changes belong in `content/`; renderer code must not special-case lesson IDs. New visual motion must preserve the distinction between application data, Kubernetes API control requests, reconciliation, lifecycle, and storage.
